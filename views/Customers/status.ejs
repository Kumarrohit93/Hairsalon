<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>My Status</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-[#fdf8f3] via-[#f7efe6] to-[#f3eadf] min-h-screen">

<!-- TOP BAR -->
<header class="sticky top-0 bg-white/70 backdrop-blur-xl border-b border-[#eadfce] px-4 py-3 flex justify-between items-center">
  <h1 class="font-semibold text-[#4a3a2f]">Dev Hair Salon</h1>
  <span class="text-sm text-[#8b7765]">Live Status</span>
</header>

<!-- MAIN -->
<main class="max-w-xl mx-auto px-4 py-6 space-y-6">

  <!-- TOKEN -->
  <section class="bg-white/80 backdrop-blur-xl border border-[#eadfce] rounded-3xl p-6 text-center shadow">
    <p class="text-sm text-[#8b7765]">Your Token Number</p>
    <h2 class="text-6xl font-bold text-[#4a3a2f] mt-2">
      #<%= customer.tokenNumber %>
    </h2>

    <p class="mt-3 text-sm font-medium 
      <%= customer.status === "waiting" ? "text-yellow-600" : customer.status === "serving" ? "text-green-600" : "text-gray-500" %>">
      Status: <%= customer.status.toUpperCase() %>
    </p>
  </section>

  <!-- CURRENT -->
  <section class="bg-white/80 backdrop-blur-xl border border-[#eadfce] rounded-3xl p-5 shadow flex justify-between items-center">
    <div>
      <p class="text-sm text-gray-500">Currently serving</p>

<div class="flex gap-2 mt-1">
  <% serving.forEach(c => { %>
    <span class="px-3 py-1 rounded-full bg-[#f5eee6] text-sm font-semibold">
      #<%= c.tokenNumber %>
    </span>
  <% }) %>
</div>
    </div>

    <div class="text-right">
      <p class="text-sm text-gray-500 mt-4">Your estimated start time</p>

<% if(customer.estimatedStartTime){ %>
  <%= new Date(customer.estimatedStartTime).toLocaleTimeString([], {hour:'2-digit', minute:'2-digit'}) %>
<% } else { %>
  Calculating...
<% } %>
    </div>
  </section>

  <!-- SERVICES -->
  <section class="bg-white/80 backdrop-blur-xl border border-[#eadfce] rounded-3xl p-5 shadow">
    <h3 class="font-semibold text-[#4a3a2f] mb-3">Your Services</h3>

    <ul class="space-y-2 text-sm text-[#6d5a4c]">
      <% customer.services.forEach(s => { %>
        <li class="flex justify-between">
          <span><%= s.name %></span>
          <span>₹<%= s.price %></span>
        </li>
      <% }) %>
    </ul>

    <div class="border-t mt-3 pt-3 flex justify-between font-semibold text-[#4a3a2f]">
      <span>Total</span>
      <span>₹<%= customer.totalAmount %></span>
    </div>
  </section>

  <!-- INFO -->
  <p class="text-center text-xs text-[#8b7765]">
    This page updates automatically when staff changes queue status.
  </p>

</main>
<script>
  localStorage.setItem("salon_public_token", "<%= customer.publicToken %>");
</script>
<% if(customer.status === "completed"){ %>
<script>
  localStorage.removeItem("salon_public_token");
  alert("Your Servixes are Completed!")
  setInterval(() => {
    window.location.reload()
  }, 30000)
</script>
<% } %>
</body>
</html>
