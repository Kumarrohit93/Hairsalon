<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Salon Admin Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-[#fdf8f3] via-[#f7efe6] to-[#f3eadf]">

  <!-- Outer glow -->
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_top,_#ffffff_0%,_transparent_60%)]"></div>

  <div class="relative w-full max-w-md">

    <!-- Card -->
    <div class="bg-white/70 backdrop-blur-2xl rounded-3xl shadow-[0_25px_60px_rgba(0,0,0,0.08)] border border-[#eadfce] p-10">

      <!-- Brand -->
      <div class="text-center mb-10">
        <div class="mx-auto w-14 h-14 rounded-2xl bg-gradient-to-br from-[#e8d9c5] to-[#d8c1a3] flex items-center justify-center shadow-md mb-4">
          ✂️
        </div>
        <h1 class="text-3xl font-semibold tracking-wide text-[#4a3a2f]"><span class="text-yellow-400">काशू</span> Hair Salon</h1>
        <p class="text-sm text-[#8b7765] mt-1">Admin Control Panel</p>
      </div>

      <!-- Form -->
      <form action="/superadmin/login" method="POST" class="space-y-6">

        <div>
          <label class="block text-sm font-medium text-[#6d5a4c] mb-2">Username</label>
          <input 
            type="text" 
            name="username"
            required
            placeholder="Enter admin username"
            class="w-full px-4 py-3 rounded-xl border border-[#eadfce] bg-[#faf7f3] focus:outline-none focus:ring-2 focus:ring-[#d2b48c] focus:border-transparent placeholder:text-[#b5a79a]"
          />
        </div>

        <div>
          <label class="block text-sm font-medium text-[#6d5a4c] mb-2">Password</label>
          <input 
            type="text" 
            name="password"
            required
            placeholder="Enter admin password"
            class="w-full px-4 py-3 rounded-xl border border-[#eadfce] bg-[#faf7f3] focus:outline-none focus:ring-2 focus:ring-[#d2b48c] focus:border-transparent placeholder:text-[#b5a79a]"
          />
        </div>

        <button 
          type="submit"
          class="w-full mt-4 py-3.5 rounded-xl bg-gradient-to-r from-[#d8c1a3] to-[#c2a27c] text-white font-semibold tracking-wide shadow-lg hover:shadow-xl hover:opacity-90 active:scale-[0.98] transition"
        >
          Enter Dashboard
        </button>

      </form>
      <p class="text-xs text-center text-[#9c8a79] mt-8">
        Secure admin access • काशू Dev Salon
      </p>

    </div>

  </div>

</body>
</html>
